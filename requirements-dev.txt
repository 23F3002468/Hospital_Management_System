# Development and test dependencies.
#
#   pip install -r requirements-dev.txt
#   pytest
#
# Kept separate from requirements.txt so a deployment does not install the test
# tooling. The suite needs no Redis and no live database - TestingConfig uses an
# in-memory SQLite and SimpleCache.

-r requirements.txt

pytest==9.1.1

# Dependency vulnerability scanner. Run before every release:
#
#   pip-audit -r requirements.txt
#
# The pins were two years stale in Aug 2026 and carried 15 known advisories.
# This turns finding that out into a command rather than an archaeology dig.
pip-audit==2.10.1
