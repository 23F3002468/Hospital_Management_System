# Flask and core extensions
Flask==3.0.0
Flask-SQLAlchemy==3.1.1
Flask-Login==0.6.3
Flask-CORS==4.0.0
Flask-Caching==2.1.0

# Database
SQLAlchemy==2.0.23

# Password hashing
Werkzeug==3.0.1

# Redis and Celery for background jobs
redis==5.0.1
celery==5.3.4

# IANA timezone database, needed by timeutils.py on Windows (which ships no tz data).
# Celery pulls this in transitively, but timeutils.py depends on it directly - pin it
# so a change to Celery's dependencies cannot break ZoneInfo('Asia/Kolkata').
tzdata==2025.2

# Email support (for monthly reports and reminders)
Flask-Mail==0.9.1

# Environment variables
python-dotenv==1.0.0

# Production WSGI server. `python app.py` starts Flask's development server,
# which must never face the internet - see the note at the bottom of app.py.
# Pure Python and cross-platform; on Linux/macOS gunicorn is the usual choice.
waitress==3.0.2

# ---------------------------------------------------------------------------
# Not currently imported anywhere - removed from the install set. Re-add if the
# matching feature is built out.
#
#   pandas==2.1.4          was listed for CSV export, but celery_worker.py uses
#                          the stdlib csv module; pandas is ~50MB for nothing
#   requests==2.31.0       was listed for Google Chat webhooks; config.py reads
#                          GOOGLE_CHAT_WEBHOOK_URL but nothing ever posts to it
#   python-dateutil==2.8.2 no direct import (celery installs it anyway)
#
# Optional: JWT authentication (if you choose JWT over session-based)
#   PyJWT==2.8.0
#   Flask-JWT-Extended==4.5.3
# ---------------------------------------------------------------------------
